---
title: Python API Reference
description: API reference for the xa11y Python package — auto-generated from type stubs.
---

{/* Auto-generated by docs/generate_python_api.py from _native.pyi — do not edit by hand. */}

The `xa11y` Python package provides bindings to the xa11y Rust library via PyO3.

Install from PyPI:

```bash
pip install xa11y
```

Quick start:

```python
import xa11y

# One-liner
tree = xa11y.app("Safari")

# With explicit provider (recommended)
with xa11y.connect() as provider:
    tree = provider.app("Safari")
    for button in tree.query("button"):
        print(button.name)
    tree.press("button[name='OK']")
```

---

## Module Functions

### `xa11y.connect() → Provider`

Create a platform accessibility provider. Use as a context manager.

### `xa11y.app(name: str | None = None, *, pid: int | None = None, max_depth: int | None = None, max_elements: int | None = None, visible_only: bool = False, roles: list[str] | None = None, include_raw: bool = False) → Tree`

Convenience function — creates a provider and returns the accessibility tree for the given app.

Identify the app by *name* (substring match) or *pid* (exact).

### `xa11y.list_apps() → list[AppInfo]`

List all running applications that expose accessibility trees.

Convenience function — creates a provider internally.

### `xa11y.check_permissions() → str`

Check whether accessibility permissions are granted.

Returns `"granted"` or raises `PermissionDeniedError`.
Convenience function — creates a provider internally.

---

## Classes

### `Provider`

The main entry point for accessibility operations.

Implements the context manager protocol.

#### Methods

| Method | Returns | Description |
| ------ | ------- | ----------- |
| `__init__()` |  | Create a new platform accessibility provider. |
| `app(name: str | None = None, *, pid: int | None = None, max_depth: int | None = None, max_elements: int | None = None, visible_only: bool = False, roles: list[str] | None = None, include_raw: bool = False)` | `Tree` | Get the accessibility tree for an application. |
| `all_apps(*, max_depth: int | None = None, max_elements: int | None = None, visible_only: bool = False, roles: list[str] | None = None, include_raw: bool = False)` | `Tree` | Get a combined accessibility tree for all running applications. |
| `list_apps()` | `list[AppInfo]` | List all running applications that expose accessibility trees. |
| `check_permissions()` | `str` | Check accessibility permission status. |
| `locator(name: str | None = None, *, pid: int | None = None, selector: str, max_depth: int | None = None, max_elements: int | None = None, visible_only: bool = False, roles: list[str] | None = None, include_raw: bool = False)` | `Locator` | Create a `Locator` bound to this provider and a target app. |

---

### `Tree`

A snapshot of an application's accessibility tree.

Iterable over all nodes in depth-first order. Supports `len(tree)`
to get the total node count.

#### Properties

| Property | Type | Description |
| -------- | ---- | ----------- |
| `app_name` | `str` | Name of the application this tree belongs to. |
| `pid` | `int | None` | Process ID of the application. |
| `screen_size` | `tuple[int, int]` | Screen dimensions as `(width, height)` in pixels. |
| `root` | `Node` | The root node of the tree. |

#### Query & Navigation

| Method | Returns | Description |
| ------ | ------- | ----------- |
| `children(node: Node)` | `list[Node]` | Get direct children of a node. |
| `parent(node: Node)` | `Node | None` | Get the parent of a node, or `None` for the root. |
| `query(selector: str)` | `list[Node]` | Find all nodes matching a CSS-like selector string. |
| `find_by_role(role: str)` | `list[Node]` | Find all nodes with the given role name. |
| `find_by_name(pattern: str)` | `list[Node]` | Find all nodes whose name contains *pattern* (case-insensitive). |

#### Action Shortcuts

These methods accept a selector string or a `Node` as *target*:

| Method | Returns | Description |
| ------ | ------- | ----------- |
| `perform(target: Node | str, action: str, *, value: str | None = None, numeric_value: float | None = None, direction: str | None = None, amount: float | None = None, start: int | None = None, end: int | None = None)` |  | Perform a named action on a node or selector target. |
| `press(target: Node | str)` |  | Press / activate an element. |
| `focus(target: Node | str)` |  | Move keyboard focus to an element. |
| `blur(target: Node | str)` |  | Remove keyboard focus from an element. |
| `toggle(target: Node | str)` |  | Toggle a checkbox or switch. |
| `expand(target: Node | str)` |  | Expand a collapsible element. |
| `collapse(target: Node | str)` |  | Collapse an expanded element. |
| `select(target: Node | str)` |  | Select an item (e.g. in a list or tab bar). |
| `increment(target: Node | str)` |  | Increment a slider or stepper. |
| `decrement(target: Node | str)` |  | Decrement a slider or stepper. |
| `show_menu(target: Node | str)` |  | Open the context menu for an element. |
| `scroll_into_view(target: Node | str)` |  | Scroll until the element is visible in its viewport. |
| `set_value(target: Node | str, value: str)` |  | Set the text value of an element. |
| `set_numeric_value(target: Node | str, value: float)` |  | Set the numeric value of an element (e.g. a slider). |
| `type_text(target: Node | str, text: str)` |  | Type text into an element. |
| `scroll(target: Node | str, direction: str, amount: float = 1.0)` |  | Scroll an element. *direction* is `"up"`, `"down"`, `"left"`, or `"right"`. |
| `select_text(target: Node | str, start: int, end: int)` |  | Select a text range by character offsets. |

#### Other

| Method | Returns | Description |
| ------ | ------- | ----------- |
| `locator(selector: str, *, max_depth: int | None = None, max_elements: int | None = None, visible_only: bool = False, roles: list[str] | None = None, include_raw: bool = False)` | `Locator` | Create a `Locator` for resilient, lazy element interaction. |
| `dump()` | `str` | Return a human-readable string representation of the full tree. |
| `__len__()` | `int` | Total number of nodes in the tree. |
| `__iter__()` | `Iterator[Node]` | Iterate over all nodes in depth-first order. |

---

### `Node`

A single element in the accessibility tree.

Nodes are immutable snapshots. Tree navigation (children, parent)
requires going through the `Tree` that produced this node.

#### Properties

| Property | Type | Description |
| -------- | ---- | ----------- |
| `role` | `str` | Role name (e.g. `"button"`, `"text_field"`). |
| `name` | `str | None` | Accessible name. |
| `value` | `str | None` | Current value (e.g. text content, slider position). |
| `description` | `str | None` | Accessible description. |
| `depth` | `int` | Depth in the tree (root = 0). |
| `numeric_value` | `float | None` | Numeric value (for sliders, progress bars, etc.). |
| `min_value` | `float | None` | Minimum numeric value. |
| `max_value` | `float | None` | Maximum numeric value. |
| `stable_id` | `str | None` | Platform-stable identifier that persists across tree snapshots. |
| `actions` | `list[str]` | List of supported action names. |
| `bounds` | `Rect | None` | Bounding rectangle in screen coordinates. |
| `bounds_normalized` | `NormalizedRect | None` | Bounding rectangle normalized to 0.0–1.0 relative to the display. |
| `enabled` | `bool` | Whether the element is interactive. |
| `visible` | `bool` | Whether the element is visible. |
| `focused` | `bool` | Whether the element has keyboard focus. |
| `checked` | `str | None` | Check state: `"on"`, `"off"`, `"mixed"`, or `None`. |
| `selected` | `bool` | Whether the element is selected. |
| `expanded` | `bool | None` | Expansion state (`None` if not expandable). |
| `editable` | `bool` | Whether the element supports text editing. |
| `focusable` | `bool` | Whether the element can receive focus. |
| `modal` | `bool` | Whether the element is a modal dialog. |
| `required` | `bool` | Whether the element is a required form field. |
| `busy` | `bool` | Whether the element is in a busy/loading state. |

#### Methods

| Method | Returns | Description |
| ------ | ------- | ----------- |
| `__len__()` | `int` | Number of direct children. |

---

### `Locator`

A resilient element reference that re-queries the tree on each interaction.

Locators are chainable and support waiting for element state changes.

#### Properties

| Property | Type | Description |
| -------- | ---- | ----------- |
| `selector` | `str` | The CSS-like selector string for this locator. |

#### Navigation

| Method | Returns | Description |
| ------ | ------- | ----------- |
| `nth(n: int)` | `Locator` | Select the *n*-th match (0-indexed). |
| `first()` | `Locator` | Select the first match (shorthand for `nth(0)`). |
| `child(selector: str)` | `Locator` | Narrow to direct children matching *selector*. |
| `descendant(selector: str)` | `Locator` | Narrow to descendants matching *selector*. |

#### Inspection

| Method | Returns | Description |
| ------ | ------- | ----------- |
| `role()` | `str` | Get the role of the matched element. |
| `name()` | `str | None` | Get the accessible name of the matched element. |
| `value()` | `str | None` | Get the current value of the matched element. |
| `description()` | `str | None` | Get the accessible description of the matched element. |
| `is_visible()` | `bool` | Check whether the matched element is visible. |
| `is_enabled()` | `bool` | Check whether the matched element is enabled. |
| `is_focused()` | `bool` | Check whether the matched element has keyboard focus. |
| `exists()` | `bool` | Check whether the selector matches any element. |
| `count()` | `int` | Count the number of elements matching the selector. |
| `get()` | `Node` | Resolve the locator to a `Node` snapshot. |

#### Actions

| Method | Returns | Description |
| ------ | ------- | ----------- |
| `press()` |  | Press / activate the matched element. |
| `focus()` |  | Move keyboard focus to the matched element. |
| `blur()` |  | Remove keyboard focus from the matched element. |
| `toggle()` |  | Toggle a checkbox or switch. |
| `expand()` |  | Expand a collapsible element. |
| `collapse()` |  | Collapse an expanded element. |
| `select_item()` |  | Select an item (e.g. in a list or tab bar). |
| `show_menu()` |  | Open the context menu for the matched element. |
| `scroll_into_view()` |  | Scroll until the matched element is visible. |
| `increment()` |  | Increment a slider or stepper. |
| `decrement()` |  | Decrement a slider or stepper. |
| `set_value(value: str)` |  | Set the text value of the matched element. |
| `set_numeric_value(value: float)` |  | Set the numeric value of the matched element. |
| `type_text(text: str)` |  | Type text into the matched element. |
| `select_text(start: int, end: int)` |  | Select a text range by character offsets. |
| `scroll(direction: str, amount: float = 1.0)` |  | Scroll the matched element. *direction* is `"up"`, `"down"`, `"left"`, or `"right"`. |

#### Waiting

| Method | Returns | Description |
| ------ | ------- | ----------- |
| `wait_visible(timeout: float = 5.0)` |  | Wait until the element is visible (default 5s timeout). |
| `wait_attached(timeout: float = 5.0)` |  | Wait until the selector matches an element (default 5s timeout). |
| `wait_detached(timeout: float = 5.0)` |  | Wait until the selector no longer matches (default 5s timeout). |
| `wait_enabled(timeout: float = 5.0)` |  | Wait until the element is enabled (default 5s timeout). |
| `wait_hidden(timeout: float = 5.0)` |  | Wait until the element is hidden (default 5s timeout). |

---

## Data Classes

### `Rect`

A bounding rectangle in screen coordinates (pixels).

#### Properties

| Property | Type | Description |
| -------- | ---- | ----------- |
| `x` | `int` | X coordinate of the top-left corner. |
| `y` | `int` | Y coordinate of the top-left corner. |
| `width` | `int` | Width in pixels. |
| `height` | `int` | Height in pixels. |

---

### `NormalizedRect`

A bounding rectangle normalized to 0.0–1.0 relative to the display.

#### Properties

| Property | Type | Description |
| -------- | ---- | ----------- |
| `left` | `float` | Left edge (0.0 = left of screen). |
| `top` | `float` | Top edge (0.0 = top of screen). |
| `right` | `float` | Right edge (1.0 = right of screen). |
| `bottom` | `float` | Bottom edge (1.0 = bottom of screen). |

---

### `AppInfo`

Information about a running application.

#### Properties

| Property | Type | Description |
| -------- | ---- | ----------- |
| `name` | `str` | Application name. |
| `pid` | `int` | Process ID. |
| `bundle_id` | `str | None` | Bundle identifier (macOS only, `None` on other platforms). |

---

## Exceptions

All xa11y exceptions inherit from `xa11y.XA11yError`.

| Exception | Description |
| --------- | ----------- |
| `XA11yError` | Base exception for all xa11y errors. |
| `PermissionDeniedError` | Accessibility permissions have not been granted. |
| `AppNotFoundError` | The target application is not running or not exposing an accessibility tree. |
| `SelectorNotMatchedError` | No element in the tree matched the given selector. |
| `ActionNotSupportedError` | The requested action is not supported on the target element. |
| `TimeoutError` | An operation exceeded its timeout. |
| `InvalidSelectorError` | The selector string has invalid syntax. |
| `PlatformError` | An OS-level accessibility error occurred. |
